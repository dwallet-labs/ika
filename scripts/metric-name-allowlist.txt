# FROZEN legacy prometheus metric names (pre-ika_-prefix convention).
# Do NOT add entries — every metric ika registers must use the ika_ prefix.
# EMPTY since the 1.2.0 full rename: the ika-native legacy names were
# renamed, the upstream-matching Sui-lineage names were renamed with them
# (operator decision at the 1.2.0 boundary), and the ~85 registered-but-
# never-set fork-residue metrics were deleted outright. The dynamic
# (format!-built) pruner names now carry the ika_ prefix in code.
