node_modules/
dist/
build/
coverage/
.turbo/
*.log
.DS_Store
.env
.env.local
.env.*.local

# Sui Move build outputs
sui/packages/contracts/**/build/
sui/packages/contracts/**/Move.lock
# `sui move summary` output. Regenerated by `bun run codegen` before TS codegen.
sui/packages/contracts/**/package_summaries/

# Next.js / frontend
**/.next/
**/out/
**/tsconfig.tsbuildinfo

# Bun
bun.lockb

# Solana / Quasar build outputs
solana/packages/program/target/

# Claude Code session state and locally-installed skills
.claude/

# Skills installed from `skills-lock.json` (akin to node_modules — content is
# downloaded on demand by the skills tool, the lockfile is the source of truth).
.agents/skills/

# Editor / OS junk
.idea/
.vscode/
*.swp
*.swo
**/.DS_Store

# Solana keypair files — never commit. Project scripts read these from
# absolute paths under /tmp or ~/.config, but guard against accidents.
*.solana-key.json
recovery-source*.json
recovery-dest*.json
recovery-publish*.json
sol-source*.json
sol-dest*.json
